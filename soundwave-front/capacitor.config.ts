import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'soundwave-front',
  webDir: 'dist/soundwave-front/browser',
};

export default config;
